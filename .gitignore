/target
/dist

# Android build + signing artifacts. NEVER commit APKs or keystores/secrets.
*.apk
*.keystore
*.jks
dist/

# Agent worktrees / harness scratch (transient; never track).
/.claude/

.idea/

# apk-pipeline: signing material never enters git
*.p12
*.pk8
*.password
secrets.env
