[package]
name = "vox-validation"
version = "0.1.0"
edition = "2024"
description = "Provenance round-trip validation and Evidence output for VoxLabs pipelines (Plan v3 D11)"
publish = false

[dependencies]
vox-core = { path = "../.." }
serde_json = "1"
